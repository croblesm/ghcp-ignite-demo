# SQL Server connection string for local container
# Replace <host>, <port>, <database_name>, <username>, and <password> with your actual values

DATABASE_URL="sqlserver://<host>:<port>;database=<database_name>;user=<username>;password=<password>;encrypt=true;trustServerCertificate=true"

# Backend API port
PORT=3000